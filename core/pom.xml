<project>
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.godaddy.vps4</groupId>
		<artifactId>vps4-parent</artifactId>
		<version>0.0.1-SNAPSHOT</version>
		<relativePath>../parent</relativePath>
	</parent>

	<artifactId>vps4-core</artifactId>
	<name>VPS4 Core</name>
	<packaging>jar</packaging>
	
	<properties>
	    <vps.env>local</vps.env>
	</properties>

	<build>
		<plugins>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>properties-maven-plugin</artifactId>
				<version>1.0.0</version>
				<executions>
					<execution>
						<phase>initialize</phase>
						<goals>
							<goal>read-project-properties</goal>
						</goals>
						<configuration>
						    <quiet>true</quiet>
						    <files>
						      <file>src/main/resources/vps4.properties</file>
						      <file>src/main/resources/vps4.${vps.env}.properties</file>
						    </files>
						</configuration>
					</execution>
				</executions>
			</plugin>
			
			<!-- Example of using antrun plugin -->
			<plugin>
			    <artifactId>maven-antrun-plugin</artifactId>
			    <version>1.8</version>
			    <executions>
			         <execution>
			             <phase>generate-resources</phase>
			             <goals>
			                 <goal>run</goal>
			             </goals>
			             <configuration>
			                 <target>
			                     <echo>Bootstrap database: ${db.vps4.server}:${db.vps4.port}/${db.vps4.bootstrap.database}</echo>
			                 </target>
			             </configuration>
			         </execution>
			    </executions>
			</plugin>
		</plugins>
	</build>

	<dependencies>

		<!-- basic dependency injection -->
		<dependency>
			<groupId>com.google.inject</groupId>
			<artifactId>guice</artifactId>
			<version>4.1.0</version>
		</dependency>
		
		<dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
            <version>2.4.3</version>
        </dependency>
        
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>9.4-1201-jdbc4</version>
        </dependency>

	</dependencies>
	
	<profiles>
	    <profile>
	        <id>rebuild-database</id>
	        <activation>
	            <activeByDefault>false</activeByDefault>
	        </activation>
	        <build>            
                <plugins>
	                <plugin>
	                    <groupId>org.codehaus.mojo</groupId>
	                    <artifactId>sql-maven-plugin</artifactId>
	                    <version>1.5</version>
	                    
	                    <dependencies>
	                        <dependency>
	                            <groupId>org.postgresql</groupId>
	                            <artifactId>postgresql</artifactId>
	                            <version>9.4-1201-jdbc4</version>
	                        </dependency>
	                    </dependencies>
	                    
	                    <configuration>
	                        <driver>org.postgresql.Driver</driver>
	                    </configuration>
	                    
	                    <executions>
	                        <execution>
	                            <id>drop-create-database</id>
	                            <phase>pre-integration-test</phase>
	                            <goals>
	                                <goal>execute</goal>
	                            </goals>
	                            <configuration>
	                                <url>jdbc:postgresql://${db.vps4.server}:${db.vps4.port}/${db.vps4.bootstrap.database}</url>
	                                <username>${db.vps4.bootstrap.username}</username>
	                                <password>${db.vps4.bootstrap.password}</password>
	                                <autocommit>true</autocommit>
	                                <delimiterType>row</delimiterType>	                                
	                                <enableFiltering>true</enableFiltering>
                                    <srcFiles>
                                        <srcFile>sql/bootstrap/drop_db.sql</srcFile>
                                        <srcFile>sql/bootstrap/create_db.sql</srcFile>
                                    </srcFiles>
	                            </configuration>
	                        </execution>
	                        <execution>
                                <id>build-database</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>execute</goal>
                                </goals>
                                <configuration>
                                    <url>jdbc:postgresql://${db.vps4.server}:${db.vps4.port}/${db.vps4.database}</url>
                                    <username>${db.vps4.username}</username>
                                    <password>${db.vps4.password}</password>
                                    <autocommit>true</autocommit>
                                    <delimiterType>row</delimiterType>
                                    
                                    <orderFile>ascending</orderFile>
                                    <fileset>
                                        <basedir>sql/migrations</basedir>
                                        <includes>
                                            <include>**/*.sql</include>
                                        </includes>
                                    </fileset>
                                </configuration>
                            </execution>
	                    </executions>
	                    
	                </plugin>
	            </plugins>
	        </build>
	           
	    </profile>
    </profiles>

</project>